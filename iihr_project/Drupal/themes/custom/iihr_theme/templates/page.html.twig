{#
  IIHR Theme — page.html.twig

  Our controller (IihrController) renders the COMPLETE page layout via
  iihr-home.html.twig / iihr-about.html.twig (top bar, header, nav, content,
  spotlight, footer — everything). This wrapper simply passes that output
  through untouched. No Drupal region blocks should interfere.
#}
{{ page.content }}